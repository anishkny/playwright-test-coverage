{
  "name": "playwright-test-coverage",
  "version": "0.0.4",
  "main": "index.js",
  "files": [
    "index.js"
  ],
  "keywords": [
    "playwright",
    "coverage",
    "istanbul",
    "babel"
  ],
  "author": "Anish Karandikar <anishkny@gmail.com>",
  "license": "MIT",
  "description": "Extends Playwright test to measure code coverage",
  "peerDependencies": {
    "@playwright/test": "^1.14.1"
  },
  "scripts": {
    "test": "./test/go.sh",
    "release": "[[ -z $(git diff --stat) ]] && standard-version || echo Please commit changes first"
  },
  "devDependencies": {
    "standard-version": "^9.3.1"
  }
}
